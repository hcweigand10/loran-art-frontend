import React from 'react'

import ReactLoading from 'react-loading';
 
const Loading = () => (
    <ReactLoading type={"spin"} color={"blue"} height={200} width={125} className="mx-auto pt-5"/>
);

export default Loading